name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Cache apt packages
        uses: actions/cache@v6
        with:
          path: /var/cache/apt/archives
          key: ${{ runner.os }}-apt-pandoc-libuv1-dev
      - name: Install pandoc and system dependencies for R packages
        run: |
          sudo apt-get update -y
          sudo apt-get install -y pandoc libuv1-dev libglpk-dev  libgdal-dev gdal-bin libproj-dev proj-bin proj-data
      - name: Generate index.html from README.md
        run: |
          pandoc README.md \
            --toc --standalone \
            --metadata title="Venezia: the living city by Hershberg & Merelo" \
            --css academic.css \
            --highlight-style tango \
            -o index.html
      - name: Set up R
        id: setup-r
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: 'release'
      - name: Install R dependencies
        run: install.packages(c("igraph","dplyr","stringr","scales","plotly","DT","visNetwork", "leaflet","jsonlite","htmltools"))
        shell: Rscript {0}
      - name: Render all Rmd and qmd files
        run: |
          for f in *.Rmd; do
            if [ -f "$f" ]; then
              Rscript -e "rmarkdown::render('${f}')"
            fi
          done
      - name: Setup Pages
        uses: actions/configure-pages@v6
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          # Upload entire repository
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
